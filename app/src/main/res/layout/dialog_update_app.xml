<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="28dp"
    android:gravity="center_horizontal"
    android:background="@drawable/dialog_background"
    android:clipChildren="false"
    android:clipToPadding="false">

    <!-- Update Icon Header -->
    <ImageView
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:src="@drawable/ic_avatar_rocket"
        app:tint="#00E676"
        android:layout_marginBottom="16dp" />

    <TextView
        android:id="@+id/tv_update_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/update_title"
        android:textColor="#FFFFFF"
        android:textSize="22sp"
        android:textStyle="bold"
        android:gravity="center"
        android:letterSpacing="0.03"
        android:layout_marginBottom="12dp" />

    <TextView
        android:id="@+id/tv_update_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/update_content"
        android:textColor="#B0BEC5"
        android:textSize="15sp"
        android:lineSpacingMultiplier="1.2"
        android:gravity="center"
        android:layout_marginBottom="24dp" />

    <!-- Update Now Button (Default Focus) -->
    <Button
        android:id="@+id/btn_update_now"
        android:layout_width="match_parent"
        android:layout_height="54dp"
        android:text="@string/update_now"
        android:textColor="#000000"
        android:textSize="16sp"
        android:textStyle="bold"
        android:background="@drawable/bg_btn_green_selector"
        android:focusable="true"
        android:nextFocusUp="@id/btn_update_later"
        android:nextFocusDown="@id/btn_update_later"
        android:layout_marginBottom="10dp" />

    <!-- Later Button -->
    <Button
        android:id="@+id/btn_update_later"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="@string/rate_later"
        android:textColor="#FFFFFF"
        android:textSize="14sp"
        android:background="@drawable/bg_btn_neutral_selector"
        android:focusable="true"
        android:nextFocusUp="@id/btn_update_now"
        android:nextFocusDown="@id/btn_update_now" />

</LinearLayout>
