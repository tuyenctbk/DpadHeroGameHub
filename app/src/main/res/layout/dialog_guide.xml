<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="750dp"
    android:layout_height="wrap_content"
    android:background="@drawable/dialog_background"
    android:orientation="vertical"
    android:padding="32dp"
    android:clipChildren="false"
    android:clipToPadding="false">

    <TextView
        android:id="@+id/guide_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/guide_title_default"
        android:textColor="#FFFFFF"
        android:textSize="32sp"
        android:textStyle="bold"
        android:shadowColor="#000000"
        android:shadowDx="2"
        android:shadowDy="2"
        android:shadowRadius="4"
        android:gravity="center"
        android:layout_marginBottom="24dp" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#44FFFFFF"
        android:layout_marginBottom="24dp" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp">
        <TextView
            android:id="@+id/guide_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/guide_content_default"
            android:textColor="#EEEEEE"
            android:textSize="20sp"
            android:lineSpacingExtra="4dp" />
    </ScrollView>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="24dp">

        <CheckBox
            android:id="@+id/cb_dont_show_again"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/dont_show_again"
            android:textColor="#BBBBBB"
            android:textSize="16sp"
            android:buttonTint="#4CAF50"
            android:focusable="true"
            android:nextFocusDown="@+id/btn_close_guide" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:clipChildren="false"
        android:clipToPadding="false">

        <Button
            android:id="@+id/btn_guide_options"
            android:layout_width="0dp"
            android:layout_height="64dp"
            android:layout_weight="1"
            android:layout_marginEnd="12dp"
            android:text="@string/game_options"
            android:background="@drawable/bg_menu_item_selector"
            android:textColor="@color/btn_menu_text_selector"
            android:textSize="20sp"
            android:textStyle="bold"
            android:elevation="4dp"
            android:focusable="true"
            android:nextFocusRight="@+id/btn_close_guide"
            android:nextFocusUp="@+id/cb_dont_show_again" />

        <Button
            android:id="@+id/btn_close_guide"
            android:layout_width="0dp"
            android:layout_height="64dp"
            android:layout_weight="1.3"
            android:layout_marginStart="12dp"
            android:text="@string/start_game"
            android:background="@drawable/bg_menu_item_selector"
            android:textColor="@color/btn_menu_text_selector"
            android:textSize="20sp"
            android:textStyle="bold"
            android:elevation="4dp"
            android:focusable="true"
            android:nextFocusLeft="@+id/btn_guide_options"
            android:nextFocusUp="@+id/cb_dont_show_again" />
    </LinearLayout>

</LinearLayout>
